# REVACHOL CrewAI 运行依赖
# 版本与 my_first_crew/.venv 当前环境一致（crewai 1.15.16 / litellm 1.97.0）
# 安装方式（Dockerfile.backend 或本地 venv）：
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# CrewAI 核心（litellm + tools extras）
crewai[litellm,tools]==1.15.16
crewai-tools==1.15.16

# LLM 网关与结构化输出
litellm==1.97.0
instructor==1.15.4
openai==2.54.0
mcp==1.28.1

# 基础库
python-dotenv==1.2.3
pydantic==2.12.5

# 终端 UI（run_revachol_crew.py 顶部仍会导入 ui.dashboard；headless 模式不渲染但依赖存在）
rich==14.3.4
prompt-toolkit==3.0.53
