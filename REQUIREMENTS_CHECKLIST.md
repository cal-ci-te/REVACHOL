# REVACHOL 需求检查清单

> 每次修改代码后，对照此清单确保没有破坏现有功能。
> 按 P0（必须）→ P1（重要）→ P2（建议）→ P3（优化）分级。

---

## 一、文章编辑模式

### 功能完整性（P0）
- [ ] 进入编辑模式后，文章内容与阅读模式一致（标题+正文+贴纸）
- [ ] 标题可编辑（通过工具栏输入框）
- [ ] 内容可编辑（contentEditable）
- [ ] 贴纸可拖拽移动（相对于文章容器）
- [ ] 贴纸右键菜单：切换浮动方向（left ↔ right）
- [ ] 贴纸右键菜单：删除贴纸
- [ ] 贴纸拖拽时边界钳制（不能超出文章容器）
- [ ] Ctrl+S 保存草稿（不退出编辑模式）
- [ ] Ctrl+Enter 发布（不退出编辑模式）
- [ ] ESC 退出（双击确认，1.5s 窗口）
- [ ] 确认保存后，数据同步到后端（`/api/articles/:id`）
- [ ] 取消编辑时，所有更改回滚到快照状态

### 状态管理（P0）
- [ ] `_article`、`_dirty`、`_snapshot` 三个状态保持同步
- [ ] 修改内容 → `_dirty = true`
- [ ] 保存成功 → `_dirty = false`，`_snapshot` 更新
- [ ] 取消编辑 → 从 `_snapshot` 恢复所有数据（标题+内容+贴纸）

### 数据持久化（P0）
- [ ] 草稿保存时携带 `stickers` 字段（JSON）
- [ ] 草稿恢复时恢复 `stickers` 数据（JSON.parse）
- [ ] 文章发布时同时更新 `stickers` 数据
- [ ] 旧草稿（无 `stickers` 字段）兼容，读取为 `[]`

### 键盘快捷键（P1）
- [ ] ESC：双击确认退出（1.5s 窗口）
- [ ] Ctrl+S：保存草稿
- [ ] Ctrl+Enter：发布
- [ ] 快捷键在 contentEditable 中不冲突

---

## 二、贴纸编辑模式

### 功能完整性（P0）
- [ ] 进入模式后，文章内容正确渲染
- [ ] 光标位置高亮（脉冲动画，2秒淡出）
- [ ] 贴纸可拖拽（相对于文章容器）
- [ ] 贴纸右键菜单：切换浮动方向
- [ ] 贴纸右键菜单：删除贴纸
- [ ] 右下角控制台显示贴纸库
- [ ] 控制台可拖拽（位置 localStorage 持久化）
- [ ] 控制台可折叠/展开
- [ ] 点击贴纸库中的贴纸，添加到文章（默认位置）
- [ ] 已放置的贴纸显示"已放置"标记
- [ ] 确认保存：贴纸数据更新到文章
- [ ] 取消：快照恢复

### 键盘快捷键（P1）
- [ ] ESC：双击确认退出（1.5s 窗口）
- [ ] Ctrl+Enter：确认保存

---

## 三、系统融合（文章编辑模式 ↔ 贴纸编辑模式）

### 入口（P0）
- [ ] 文章编辑模式工具栏：点击"📌 贴纸" → 打开贴纸编辑模式
- [ ] 贴纸编辑模式确认保存 → 文章编辑模式贴纸层更新
- [ ] 贴纸编辑模式取消 → 文章编辑模式贴纸层不变

### 状态同步（P0）
- [ ] 贴纸编辑模式保存后 → 文章编辑模式的 `_stickerData` 更新
- [ ] 文章编辑模式保存草稿 → 贴纸数据随草稿存储
- [ ] 文章编辑模式恢复草稿 → 贴纸数据恢复

### 数据一致性（P0）
- [ ] 贴纸坐标在两个模式中一致（相对于文章容器）
- [ ] 贴纸形状（16边形）在两个模式中一致

---

## 四、主题适配

### CSS 变量覆盖（P0）
- [ ] 三套主题（dark/light/lofi）都适配
- [ ] 所有颜色使用 `var(--color-*)`，无硬编码色值
- [ ] 编辑模式覆盖层背景色跟随主题
- [ ] 文章内容文字颜色跟随主题
- [ ] 工具栏背景色跟随主题
- [ ] 侧边栏背景色跟随主题
- [ ] 贴纸边框颜色跟随主题

### 主题切换（P1）
- [ ] 切换主题时，编辑模式中的颜色同步更新
- [ ] 切换主题时，贴纸样式同步更新

---

## 五、移动端

### 功能禁用（P0）
- [ ] ≤768px 隐藏"添加贴纸"按钮
- [ ] ≤768px 禁用文章编辑模式（显示友好提示）
- [ ] ≤768px 禁用贴纸编辑模式（显示友好提示）
- [ ] ≤768px 禁用拼图组件

### 响应式（P2）
- [ ] 文章内容在移动端正常显示（字体大小、行距）
- [ ] 侧边栏在移动端可正常折叠/展开

---

## 六、性能与内存

### 内存泄漏（P0）
- [ ] 拖拽事件（mousedown/mousemove/mouseup）在结束时正确解绑
- [ ] 右键菜单的点击事件在菜单关闭后清理
- [ ] 控制台拖拽的事件监听器在模式关闭时移除
- [ ] 光标高亮的定时器在模式关闭时清除

### 性能（P1）
- [ ] 16边形 shape-outside 在 5 个贴纸时保持 60fps
- [ ] 形状生成有缓存（避免重复计算）
- [ ] 拖拽贴纸时不触发不必要的重排/重绘

---

## 七、错误处理

### 数据错误（P1）
- [ ] 贴纸库为空时，控制台显示友好提示
- [ ] 文章内容为空时，编辑模式显示占位符
- [ ] 贴纸图片加载失败时，显示占位符
- [ ] 保存草稿时 API 失败 → 显示错误 Toast
- [ ] 发布时 API 失败 → 显示错误 Toast

### 用户操作（P1）
- [ ] 未保存更改时 ESC → 弹出确认对话框
- [ ] 未保存更改时点击关闭 → 弹出确认对话框
- [ ] 双击 ESC 确认机制：首次显示提示，第二次确认

---

## 八、文档与规范

### 代码规范（P2）
- [ ] 所有 UI 文案在 `js/utils/ui-strings.js` 中
- [ ] 所有事件常量在 `js/core/event-constants.js` 中
- [ ] 核心函数有 JSDoc 注释
- [ ] 文件头有模块职责说明

### 文档（P2）
- [ ] `docs/development/component-manager.md` 已更新
- [ ] `docs/development/sticker-editor.md` 已更新
- [ ] `README.md` 版本号已更新
- [ ] `CHANGELOG.md` 已记录本次改动

---

## 检查方法

每次修改后，执行以下验证：

例：

```bash
# 1. 功能验证（手动）
# 进入编辑模式 → 修改标题/内容 → 拖拽贴纸 → 保存草稿 → 刷新 → 恢复

# 2. 主题验证
# 切换三套主题，检查颜色一致性

# 3. 内存验证
# 打开/关闭编辑模式 5 次，检查监听器数量
# Chrome DevTools → Performance → Event Listeners

# 4. API 验证
# Chrome DevTools → Network → 确认草稿保存/发布请求正确
```